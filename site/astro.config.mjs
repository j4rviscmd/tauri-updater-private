// @ts-check
import { defineConfig } from 'astro/config';

// Repository Pages URL. Update `site` if a custom domain is added later.
export default defineConfig({
  site: 'https://j4rviscmd.github.io',
  base: '/tauri-updater-private',
});
